# CrashGuard — Market Crash Spotter

Run locally or on GitHub Actions to compute a Crash Risk Index (CRI) and send alerts.
See `config.yaml` for thresholds/weights. Use `.env.example` for credentials.
